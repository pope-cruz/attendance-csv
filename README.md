# tech@nyu attendance

Internal attendance reconciliation for Luma and NYU Engage exports.

## Setup

Copy `.env.example` to `.env.local`, fill in the Supabase project values, and
install dependencies before running `npm run dev`.

For the shared organizer password and authenticated database policies, follow
[`docs/shared-access-setup.md`](docs/shared-access-setup.md).
