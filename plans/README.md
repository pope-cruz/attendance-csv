# Implementation Plans

Generated by the `improve` skill on 2026-08-07. Execute one plan at a time in
the order below. Each executor must read its plan fully, honor its STOP
conditions, run every verification command, and stop when that milestone is
complete.

## Execution order and status

| Plan | Title | Priority | Effort | Depends on | Status |
|---|---|---:|---:|---|---|
| 001 | Make attendance and member counts ambiguity-safe | P1 | S | — | DONE |
| 002 | Make Supabase event saves atomic and retry-safe | P1 | M | 001 | IN PROGRESS — code complete; awaiting Supabase preflight and schema deployment |
| 005 | Preserve validation issues through the Supabase round trip | P1 | M | 002 | TODO |
| 003 | Lock attendance data behind an email allowlist | P1 | L | 002, 005 | TODO |
| 004 | Restore lint and keyboard access on Members | P2 | S | 001 | DONE |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED` with a one-line reason,
or `REJECTED` with a one-line rationale.

## Dependency notes

- Execute 001 first because trustworthy attendance semantics are the core
  product requirement and later smoke tests depend on those semantics.
- Execute 002 before 003 because the database RPC introduced by 002 must be
  covered by the authenticated RLS policy introduced by 003.
- Execute 005 after 002 and before 003. It extends the RPC and event schema
  introduced by 002 so file-level validation issues survive save and reload;
  auth should be applied only after that persistence contract is complete.
- Plan 005 is P1 because silently losing validation evidence conflicts with
  the app's core correctness and source-truth requirements.
- Plan 004 may technically run after 001 in parallel with database work, but
  this repository intentionally ships one small milestone at a time.
- Do not combine these plans into one PR. The review and rollback surface would
  become too large for this small internal tool.

## How to hand a plan to an unstructured agent

Use a prompt this small:

> Implement `plans/001-attendance-correctness.md` exactly. Read `AGENTS.md` and
> the entire plan first. Modify only files listed as in scope, run every
> verification command, update the plan status, summarize every modified file,
> and stop when the plan is complete. If a STOP condition occurs, report it
> instead of improvising.

Replace `001-attendance-correctness.md` with the next plan only after the prior
plan has been reviewed and marked `DONE`.

## Findings considered and deferred

- Stale documents in `docs/parsing-overview.md` and
  `docs/supabase-checklist.md`: valid problem, but update them after the P1
  persistence and access plans so they describe the final behavior once
  instead of churning after every repair.
- Default Turbopack build panic while binding a local port: not treated as an
  application defect because `npm run typecheck` and
  `npm run build -- --webpack` pass; re-evaluate only if it reproduces outside
  the restricted agent environment.
- Supabase read pagination: the current all-rows load can eventually hit a
  project response limit, but the tool is intentionally small. Measure stored
  event-row volume before adding pagination or SQL aggregation.
- Next.js and `eslint-config-next` minor-version mismatch: no observed runtime
  failure. Align versions during a separate dependency-maintenance task, not
  during correctness work.

## Audit boundary

These plans were produced from the local repository at commit `4411f449`.
Live Supabase project settings, current RLS deployment, Vercel configuration,
and production data were not inspected.
